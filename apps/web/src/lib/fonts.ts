import { Inter, DM_Serif_Display } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const dm_sefif_display = DM_Serif_Display({
  subsets: ["latin"],
  weight: "400",
  display: "swap",
});
