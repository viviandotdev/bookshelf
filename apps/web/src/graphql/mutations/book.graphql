mutation CreateBook($data: BookCreateInput!) {
  createBook(data: $data) {
    author
    categories
    coverImage
    description
    id
    pageCount
    publishedDate
    publisher
    title
  }
}
