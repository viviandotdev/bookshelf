query readDates($userBookIds: [String!]!, $active: Boolean) {
  readDates(userBookIds: $userBookIds, active: $active) {
    id
    userBookId
    startDate
    finishedDate
    active
    readingProgress {
      id
      capacity
      type
      progress
    }
  }
}
