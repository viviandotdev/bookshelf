query Reads($where: ReadWhereInput) {
  reads(where: $where) {
    id
    startDate
    finishedDate
    userBookId
    capacity
    readingSessions {
      id
      startPage
      endPage
      createdAt
    }
  }
}
