query Me {
  me {
    username
    email
    name
    location
    bio
    avatarImage
  }
}

query GetLoginOptions($email: String!) {
  getLoginOptions(email: $email) {
    hasAccount
    passwordSignIn
  }
}
