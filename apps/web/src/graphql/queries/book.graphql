query Book($where: BookWhereUniqueInput!) {
  book(where: $where) {
    id
    title
    authors {
      name
    }
    coverImage
    description
    publishedDate
    publisher
    pageCount
    work {
      categories
      averageRating
      ratingsCount
    }
  }
}
