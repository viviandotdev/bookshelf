query Book($where: BookWhereUniqueInput!) {
  book(where: $where) {
    id
    slug
    title
    authors
    pageCount
    identifiers {
      source
      sourceId
    }
    covers {
      url
      size
    }
    ratings {
      source
      score
    }
  }
}
