query Book($where: BookWhereUniqueInput!) {
  book(where: $where) {
    id
    userBook {
      id
    }
    slug
    title
    authors
    pageCount
    identifiers {
      source
      sourceId
    }
    covers {
      url
      size
    }
    ratings {
      source
      score
    }
  }
}
